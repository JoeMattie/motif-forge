# TODO — unimplemented plans

Nothing pending — every written plan has shipped. Implemented/superseded plans live in [archive/](archive/) (most recently the chords/voicing switch and the M6(GPT)3 insights: mood knobs, Claude-as-planner, chord scaffold, and the extra mutation ops).

When a new feature gets planned, add its plan file to this folder and link it here; move it to archive/ once it lands.
