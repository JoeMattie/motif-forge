Main:

- Grid / focus selector should be to the left of the triage/library/concepts selector so it doesn't jump when switching tabs
- day/night selector should be icons not words
- hotkeys highlighted in red should be just the text color. right now it is affecting the layout (the F in Family is offset from the rest of the word)

Triage view:

- Highlight outline should be above the family stack.
- keyboard arrow keys should navigate logically, not just left right (down should move to the one below, not the next to the right) etc
- in family panel, all tracks should be same width

Mutation:
- This process isn't making sense. What we want to do is select a track and open the mutate panel. It should show all the subtracks. Each subtrack should have a mutate button and an advanced button. Clicking advanced opens up the current transform panel, mutate essentially just generates several variations (5) of only that track, which are then nested to the right connected by lines to the track they came from. Arrow keys navigate around, enter selects the current mutation and that is used when playing. space  Playing (spacebar) plays the entire thing in a loop, eg: the mutations that are selected. So each instrument can be mutated separately, played in a loop until the right variation is found. There should be a big red button at the bottom right that promotes whatever is selected right now in its entirety to a new track in the family of the source. The cable legend is superfluous. The A B loop thing is confusing. Also, mutations should be able to be mutated as well in a tree. Ask me questions if any of this doesn't make sense